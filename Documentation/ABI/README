> 🌐 本文档由 [torvalds/linux](https://github.com/torvalds/linux) 翻译,英文原版见原项目。

Documentation/ABI 目录下的这部分文档,旨在描述 Linux 内核与用户空间之间的 ABI,以及这些接口的相对稳定性。由于 Linux 时刻处于演变之中,且各接口的成熟程度参差不齐,用户空间程序应当以不同的方式来使用这些接口。

我们共有四种不同的 ABI 稳定性级别,对应本目录下四个不同的子目录。接口可以按照下述规则在不同稳定性级别之间迁移。

各稳定性级别如下:

  stable/
	本目录记录开发者已定义为稳定的接口。用户空间程序可以不受任何限制地自由使用这些接口,并且其向后兼容性将获得至少 2 年的保证。大多数接口(例如系统调用)预期永远不会改变,并且始终可用。

  testing/
	本目录记录被认为已经稳定的接口,即该接口的主要开发工作已经完成。接口可以为进一步添加新特性而变更,但这样做不会破坏当前接口,除非在其中发现了严重错误或安全问题。用户空间程序可以开始依赖这些接口,但必须意识到:在这些接口被标记为 stable 之前,仍可能发生变更。强烈建议使用这些接口的程序将自己的名称添加到这些接口的描述中,以便内核开发者在发生任何变更时能够方便地通知它们(具体做法参见下文对文件布局的说明)。

  obsolete/
	本目录记录仍然保留在内核中、但已被标记将在未来某个时间点移除的接口。接口的描述中会说明其被废弃的原因,以及预计何时会被移除。

  removed/
	本目录包含已从内核中移除的旧接口的清单。

这些目录中的每个文件都包含以下信息:

What:		接口的简短描述
Date:		创建日期
KernelVersion:	(可选)该特性首次出现的内核版本。
		注意:git 历史往往能提供更准确的版本信息,因此该字段可以省略。
Contact:	该接口的主要联系方式(可以是邮件列表)
Description:	接口的详细描述及其使用方法。
Users:		所有希望在接口发生变更时得到通知的该接口的使用者。
		对于处于 "testing" 阶段的接口而言,这一点尤为重要,这样内核
		开发者才能与用户空间开发者协作,确保不会出现不可接受的
		破坏。同时,获取这些接口的反馈也很重要,以确保它们以正确的
		方式工作,并且不需要进一步修改。


注意:
   这些字段应使用简单的记法,并与 ReST 标记兼容。此外,文件**不应**
   包含顶层索引,例如::

	===
	foo
	===

接口在各稳定性级别之间如何迁移:

只要给出了妥当的通知,stable 中的接口就可以迁移到 obsolete。

只要文档中记载的时限已经届满,接口就可以从 obsolete 以及内核中移除。

处于 testing 状态的接口,在开发者认为其已经完成时,可以迁移到 stable 状态。它们必须先经历 obsolete 状态,才能从内核代码树中移除。

将接口初始归入哪个类别,由开发者自行决定。


以下是一些值得注意的非 ABI 内容,在任何情况下都不应将其视为稳定:

- Kconfig。用户空间不应依赖任何特定 Kconfig 符号的存在与否,无论它出现在 /proc/config.gz 中、通常安装到 /boot 的 .config 副本中,还是内核构建过程的任何一次调用中。

- 内核内部符号。不要依赖任何内核符号的存在与否、位置或类型,无论是在 System.map 文件中,还是在内核二进制文件本身中。参见 Documentation/process/stable-api-nonsense.rst。
